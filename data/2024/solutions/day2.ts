export default {
  part1: (input: string): number => {
    const lines = input.split('\n');
    return 0;
  },
  
  part2: (input: string): number => {
    const lines = input.split('\n');
    return 0;
  }
};